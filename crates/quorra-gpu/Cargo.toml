[package]
name = "quorra-gpu"
description = "The device half of the quorra document renderer: wgpu, pipelines, atlas, frames."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# Nothing is declared yet, and that is deliberate rather than an oversight: no module in
# this crate contains code (doc/adr/0003), so a dependency here would be a dependency
# compiled for nobody. `wgpu`, `thiserror` and `pollster` are pinned and argued for in the
# workspace manifest and in doc/adr/0002; M1 references them from here with
# `dep.workspace = true` in the commit that first calls one.
[dependencies]

[lints]
workspace = true
